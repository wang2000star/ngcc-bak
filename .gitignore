# ---------------------------------------------------------------------------
# 测试向量只在本地保留，不上传 GitHub。
# 原因：体积大（约 7.2 GiB，占全部内容的 70%），且是十六进制文本，
#       压缩比已接近理论上限（~2x），放进仓库收益低、代价高。
# 本地文件不会被删除，只是不纳入 Git 跟踪。
# ---------------------------------------------------------------------------
Test_Vectors/
Test_Vector/
*.rsp
KAT*.txt
kat*.txt
