# bike_kem_ml

This trimmed copy is part of `bikekem_v2`.

Use the top-level `README_BIKEKEM_V2.md` and prefer security-bit targets:

- `SECURITY_BITS=128`
- `SECURITY_BITS=256`
- `SECURITY_BITS=512`

The old BIKE internal levels are kept only as implementation details.

Top-level scripts are intentionally minimal:

- `test-bike-kem-128-base.sh`
- `test-bike-kem-256-base.sh`
- `test-bike-kem-512-base.sh`
- `test-bike-kem-512-ml.sh`
- `collect-bike-kem-perf-security.sh`
