# Additional Implementation

No additional implementation is submitted for LoomKEX at this time.

Per NGCC submission requirements, this directory is reserved for optional third-tier implementations (e.g. platform-specific or alternative-language ports). When an additional implementation is provided, each algorithm instance shall reside in its own subdirectory named after the instance (e.g. `LoomKEX-128/`).
