set(SOURCE_FILES_COMMON_${SVARIANT_UPPER}
    ${COMMON_SRC_DIR}/common/drng.c
)

set(INC_COMMON_${SVARIANT_UPPER} ${COMMON_SRC_DIR}/common/include CACHE INTERNAL "INC")
set(LIB_COMMON_${SVARIANT_UPPER} niike_common_${SVARIANT} CACHE INTERNAL "LIB")
add_library(${LIB_COMMON_${SVARIANT_UPPER}} STATIC ${SOURCE_FILES_COMMON_${SVARIANT_UPPER}})
target_include_directories(${LIB_COMMON_${SVARIANT_UPPER}} PRIVATE ${INC_COMMON_${SVARIANT_UPPER}})
target_compile_options(${LIB_COMMON_${SVARIANT_UPPER}} PRIVATE ${C_OPT_FLAGS})
