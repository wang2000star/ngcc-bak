# SHA3-512 Reference C (XKCP readable)

This baseline wraps the official XKCP source:

`XKCP-master/Standalone/CompactFIPS202/C/Keccak-readable-and-compact.c`

The upstream file is copied unchanged as `Keccak-readable-and-compact.c`.
The wrapper `benchmark_sha3_512_reference_c.c` adds benchmark and KAT CLI
metadata without modifying the official source.
