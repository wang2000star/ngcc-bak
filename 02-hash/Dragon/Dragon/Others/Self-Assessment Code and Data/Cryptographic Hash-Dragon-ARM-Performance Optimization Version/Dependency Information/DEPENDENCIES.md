# Dependency Information

Package: `Cryptographic Hash-Dragon-ARM-Performance Optimization Version`

## Third-Party Libraries

Dragon is a cryptographic hash algorithm implementation and this package does not use any third-party cryptographic library.

The algorithm implementation is self-contained C source code. It depends only on the standard C library and the normal system toolchain used to compile and run the tests.
