# 密码杂凑-Eijen-x86-性能优化版自评估报告

本文档是 `密码杂凑-Eijen-x86-性能优化版` 的 x86 架构实现自评估报告，作为提交包内该实现版本的可复核技术证据。

## 1. 算法基本信息

- 算法类别：密码杂凑算法
- 算法名称：Eijen
- 细分功能：杂凑值生成
- 实现版本：性能优化版
- 参数集：Eijen-256、Eijen-384、Eijen-512、Eijen-768、Eijen-1024
- 输出摘要长度：32、48、64、96、128 bytes

## 2. 材料目录

- 算法源代码：`algorithm_source/include/eijen.h`、`algorithm_source/src/eijen_avx2.c`、`algorithm_source/src/eijen_internal.h`
- 自评估代码：`self_assessment/tests/eijen_kat.c`、`self_assessment/tools/eijen_bench.c`
- 自评估数据：`self_assessment/data/eijen_kat.tsv`
- 自评估脚本：`self_assessment/run_self_assessment.sh`
- 原始结果：`self_assessment/results/`
- 依赖说明：`docs/dependencies.md`

## 3. 实现说明

x86-64 性能优化实现，单消息路径使用 O3/LTO 优化，四路等长短消息接口使用 AVX2 intrinsic。

编译配置为：`gcc -O3 -march=x86-64 -mavx2 -mtune=native -flto -fomit-frame-pointer -std=c99 -Wpedantic -Wall -Wextra`。

## 4. 评估环境信息

- 服务器：`240:~/eijen-submit/密码杂凑-Eijen-x86-提交包`
- 操作系统与内核：`Linux ktjia-Precision-7960-Rack 6.8.0-106-generic #106~22.04.1-Ubuntu SMP PREEMPT_DYNAMIC Fri Mar  6 08:44:59 UTC  x86_64 x86_64 x86_64 GNU/Linux`
- CPU：Intel(R) Xeon(R) Silver 4416+
- 架构：x86_64
- AVX2：支持
- 编译器：`gcc (Ubuntu 11.4.0-1ubuntu1~22.04.3) 11.4.0`
- 构建工具：`cmake version 3.22.1`
- 单核测试：`self_assessment/run_self_assessment.sh` 默认使用 `taskset -c 0`
- 结果生成时间：`2026-06-29T10:07:35Z`

完整环境记录见 `self_assessment/results/environment.txt`。

## 5. 测试命令

在本目录下执行：

```sh
self_assessment/run_self_assessment.sh
```

该脚本会执行以下复核步骤：

```sh
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build -j 1
ctest --test-dir build --output-on-failure
build/bench > self_assessment/results/bench.csv
```

并同步生成 `environment.txt`、`static_size.txt` 和 `time.txt`。

## 6. 功能测试

KAT 测试覆盖：

- Eijen-256、Eijen-384、Eijen-512、Eijen-768、Eijen-1024 的 zero/one/abc 测试
- 上述全部参数集的 `abc` 流式接口分片测试
- 上述全部参数集的长消息 one-shot/streaming 一致性测试
- 上述全部参数集的 bit-length API 一致性测试
- `eijen_hash4_same` 四路等长短消息 AVX2 接口正确性测试

预期向量记录在 `self_assessment/data/eijen_kat.tsv`。服务器执行结果为 `Passed`，原始日志见 `self_assessment/results/kat.log`。

## 7. 性能测试

性能测试覆盖 x86 指引规定的 S1-S8 输入长度：

| 级别 | 输入长度 |
| --- | ---: |
| S1 | 32 Bytes |
| S2 | 128 Bytes |
| S3 | 512 Bytes |
| S4 | 1024 Bytes |
| S5 | 4096 Bytes |
| S6 | 8192 Bytes |
| S7 | 16384 Bytes |
| S8 | 65536 Bytes |


`self_assessment/tools/eijen_bench.c` 对每个测试项执行 `repetitions=5` 次重复，并在每次重复内执行 CSV 中记录的 `iterations` 次核心杂凑操作；所有 S1-S8 测试项的 `iterations` 均不小于 200。cycle 计数使用序列化 TSC 读数，并输出 digest checksum，避免测量循环被优化弱化。完整明细见 `self_assessment/results/bench.csv`。

摘要结果如下：

| 参数集 | S1 iterations | repetitions | S1 cycles/hash | S1 MiB/s | S8 iterations | S8 cycles/hash | S8 MiB/s |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| Eijen-256 | 65536 | 5 | 537.06 | 113.65 | 200 | 132252.75 | 945.16 |
| Eijen-384 | 65536 | 5 | 569.67 | 107.14 | 200 | 143050.08 | 873.82 |
| Eijen-512 | 65536 | 5 | 600.11 | 101.71 | 200 | 156269.81 | 799.90 |
| Eijen-768 | 65536 | 5 | 663.72 | 91.96 | 200 | 189090.21 | 661.06 |
| Eijen-1024 | 65536 | 5 | 728.66 | 83.76 | 200 | 239693.84 | 521.50 |

## 8. 资源消耗测试

静态尺寸由 `size build/kat build/bench` 生成，峰值内存由 `/usr/bin/time -v build/bench` 生成。原始证据见 `self_assessment/results/static_size.txt` 和 `self_assessment/results/time.txt`。

| 目标 | text | data | bss | dec |
| --- | ---: | ---: | ---: | ---: |
| `kat` | 19097 | 1136 | 8 | 20241 |
| `bench` | 8405 | 792 | 16 | 9213 |

| 目标 | Maximum resident set size |
| --- | ---: |
| `bench` | 1600 KB |

实现侧内存占用还包括：

- `sizeof(eijen_ctx) = 488` bytes；
- 单消息吸收每次最多使用 `saved_cap[17] = 136` bytes 栈临时区；
- 性能优化版四路短消息快路径使用 AVX2 状态数组，约 `32 * 32 = 1024` bytes 栈空间；参考实现版和资源优化版不使用该四路 AVX2 快路径。

## 9. 传输与存储开销

Eijen 是密码杂凑算法，不涉及公钥、私钥、签名、密文或密钥交换消息尺寸；本项不适用。输出摘要长度为 32、48、64、96、128 bytes。

## 10. 原始证据索引

- `self_assessment/results/environment.txt`：测试环境
- `self_assessment/results/kat.log`：KAT 执行日志
- `self_assessment/results/bench.csv`：本实现版本 S1-S8 性能数据
- `self_assessment/results/static_size.txt`：静态尺寸
- `self_assessment/results/time.txt`：峰值内存
